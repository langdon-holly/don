#!/usr/bin/env don

#\print\->str(\str->unicode "hi 1)

\print (->list (fn "a "a) 100)

# function function fn test
(fn "2*
  "(fn "*3
     "(2* *3 5)
     (fn "x "\+[x x x]))
  (fn "a-fn
    "(fn "x
       "\a-fn \a-fn x)))

\print{| \->str\+[5 3]|`a`kad|||"hello|} # syntax test

