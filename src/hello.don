#!/usr/bin/env don

(print (use 'use-testee.don ))

;(print (path-from-dir (dir-of src-path )'use-testee.don ))

;(print (flatten [(dir-of 'src/hello.don )[10 ](base-of 'src/hello.don )]))

;(print (make-ident src-path-var-sym env ))

("(unok (parse-prog-expr source-data )env )(use \'std-env.don|env ))

(print 'OK )
