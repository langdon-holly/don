#!/usr/bin/env don

[ (say (flatten [[][0 1 ][2 3 4 ][5 ]]))
  (print (flatten ['first-line [10 ]'second-line ]))
  (print
    (flatten
      [ (utf8-encode 378 )
        (utf8-encode 339 )
        (utf8-encode 960 )
        (utf8-encode 920 )]))
  (say (reverse-concat [1 0 ][2 3 4 ]))
  (print (reverse-concat 'Hello []))]

("(unok (parse-prog-expr source-data )env )(eval-file \'std-env.don|env ))

(print 'OK )
