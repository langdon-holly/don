#!/usr/bin/env don

("(unok (parse-prog-expr source-data )env )(eval-file |"std-env.don|env ))

(fn "counter
  "[ (async "(continue counter 2 ))
     (async "(continue counter 4 ))
     (continue counter 3 )]
  (call/onerr
    make-sync-cont
    (Z
      (fn "add
        "(fn "a "(fn "b "(false (say (+ [a b ])) (add (+ [a b ]))))))
      0 )))

\

HELO
